node_modules
dist
.next
coverage
pnpm-lock.yaml
*.min.js
*.min.css

# Helm Go templates are not valid YAML for Prettier
infrastructure/helm/**/templates/**

# Generated / large baseline artifacts
tools/dod-checks/reports/baseline.json
tools/dod-checks/reports/dod-report.json
*.tsbuildinfo

# Playwright / visual artifacts (binary + ephemeral)
**/test-results/**
**/playwright-report/**
**/*.spec.ts-snapshots/**
*.png
*.zip

# G-804 generated page-wise regression matrix (tools/scripts/page-regression-matrix.mjs)
docs/testing/PAGE_REGRESSION_MATRIX.md

# UI->API->DB static trace tool (PROCTIRA_FIX_LEDGER). Deliberately dense/compact
# one-liner style throughout; running prettier --write would reformat every line
# of someone else's tooling script into a huge unrelated diff instead of just the
# targeted fix. Consistent with the other tooling exclusions above.
docs/multitenant/trace-graph.mjs
