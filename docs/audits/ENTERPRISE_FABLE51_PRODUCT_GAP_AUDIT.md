# ProctiraERP — Master Enterprise Gap Report (single source of truth)

**Date:** 2026-09-08 · **Auditor:** Fable 5.1 (parallel explores + repo verification) · **Base:** `main` @ `39e899f`  
**Purpose:** The one report Cursor Auto-mode agents work from, **gap by gap, in order**, until ProctiraERP is a world-class enterprise school ERP.  
**Skills that govern every task:** `.cursor/skills/enterprise-module-production-ready/SKILL.md` and the related skills it lists (security-tenancy, data-sql-certification, product-ia, ux-designer, accessibility, mobile-flutter, release-ops, module-development).

---

## 0. How agents must use this report

1. Pick the **lowest-numbered OPEN gap** whose `Depends on` are all `DONE`.
2. Work on a slim branch `cursor/<gap-id-lowercase>-56c3` from `origin/main`. One gap = one PR.
3. Meet the **Acceptance** exactly; attach evidence paths (tests, artifacts, audit md).
4. Update the **Status** column here (`OPEN → IN PROGRESS → DONE (PR #n)`) in the same PR.
5. Never claim a pillar that needs external secrets (IdP, PSP, Twilio, LMS, Statuspage) — mark **WAIVED (dated)**.
6. Do not open mega-PRs. PR #38 (optional Keycloak) is the reference size.

---

## Progress log

| Date       | Wave          | Closed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-08 | 0–1           | G-002, G-003, G-101, G-102, G-103, G-104 on PR #40 (`cursor/enterprise-gap-close-56c3`)                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-09-08 | 2             | G-105 DONE; G-204/205 DONE (pg repos); G-401 scaffolding; G-201 fees still OPEN                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-09-08 | 2b            | **G-201 DONE** — `@proctira/backend-fees` + gateway `/fees` mount                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-09-08 | 2c            | **G-203/206/207/208 DONE** — special-needs PHI + leave balances + notification deliveries PG + workflow-ui PG store                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-08 | 4 (partial)   | G-401 scaffolding: `.github/workflows/e2e-backend-ready.yml` + `tools/scripts/run-e2e-backend-ready.sh` (migrate/SQL + HS256 health write smoke; seeded-login journeys still residual)                                                                                                                                                                                                                                                                                                                                      |
| 2026-09-08 | 2d / 4        | **G-209 DONE** (insights PG `020`); **G-210 DONE** (report-card routes wired); **G-401 DONE** (harness + nightly job; residual seeded-login journeys); **G-402 DONE** (HS256 axe path + 17c in G-401)                                                                                                                                                                                                                                                                                                                       |
| 2026-09-08 | 3             | **G-301 DONE** (campus RBAC deny matrix + e2e route list); **G-302 DONE** (human labels / search-select on timetable·schedule·gradebook); **G-303 DONE** (grade workflow + transcript HMAC stub); **G-305 DONE** (async board export + signed download + tenant deny)                                                                                                                                                                                                                                                       |
| 2026-09-08 | 3 / 4 / 5     | **G-304 DONE** (conflicts API/UI + bulk enroll + property tests); **G-306 DONE** (JWT parent journeys + forge deny + G-401 harness); **G-403 DONE** (Playwright screenshot baselines + CI); **G-501 DONE** (`proctira-platform` rename + `proctira-service` + helm template CI); **G-502 DONE** (SLO doc + critical_journeys alerts)                                                                                                                                                                                        |
| 2026-09-08 | 6 / 3 / 4 / 5 | **G-602 DONE** (GPS/attendance-on-bus stubs + write smoke); **G-603 DONE** (fines→fees ledger); **G-604 DONE** (sandbox delivery + RBAC send deny + audit); **G-605 PARKED** (survey/custom-field/dashboards/theme + matrix test); **G-606 DONE** (min packs evidence); **G-607 DONE** (developer portal mounted AuthZ/keys); **G-307 DONE** (dry-run + transactional flag); **G-405 DONE** (campus i18n parity lint); **G-507 DONE** (install wizard onboarding harness). Residual: G-001 Keycloak; G-407 farm WAIVED/OPEN |
| 2026-09-08 | 0 (G-001)     | **G-001 DONE** on PR #40 — merged `cursor/keycloak-auth-optional-56c3` (PR #38): env-gated Keycloak RS256 + MFA/invite routes + `.env.example` / realm JSON / Phase-2 signoff. Residual: persist identity/invite/OTP; live IdP = G-107 WAIVED until secrets. G-407 farm remains WAIVED.                                                                                                                                                                                                                                     |

## 1. Verdict

| Pillar                     |                                                                                   Score /10 | Headline gap                                                                                                                                                                                                                |
| -------------------------- | ------------------------------------------------------------------------------------------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Security & tenancy         |                                                                                     **3.5** | RBAC plugin exists but is **never mounted**; actors readable from `x-user-id` headers; raw-SQL tables have **zero RLS**                                                                                                     |
| Data / persistence         |                                                                                     **4.0** | CI applies **Prisma only**; `db/sql/001–014` never applied in CI → those modules silently run **in-memory** in tests; scholarships, admissions, special-needs, notifications, workflows, insights, platform-admin in-memory |
| Functionality              |                                                                                     **5.0** | Fees module = SQL file only, no gateway plugin; report cards disabled; HR leave / admissions not persisted                                                                                                                  |
| UX / a11y / multidevice    |                                                                                     **5.5** | Authenticated axe / dark / touch / RTL still thin; desktop visual-diff baselines + CI job added (G-403)                                                                                                                     |
| Platform / admin / release |                                                                                     **4.5** | Platform-admin gated; Helm renamed to `proctira-platform` + `proctira-service` with `helm template` CI; secrets rotation / DR still open                                                                                    |
| E2E evidence               |                                                                                     **4.0** | `E2E_BACKEND_READY` absent from all workflows → write journeys never run in CI                                                                                                                                              |
| **Overall**                | **~5.5 / 10** (Wave 0–1 security spine closed on branch `cursor/enterprise-gap-close-56c3`) | Solid architecture and skills culture; **controls and persistence are not composed into the running gateway**                                                                                                               |

Peer-gap queue #1–#10 (closed) fixed peer deltas. This report covers the **whole product**.

---

## 2. Gap register (work top to bottom)

Legend — **Sev:** S0 blocker · S1 critical · S2 major · S3 minor. **Status:** OPEN unless noted.

### Wave 0 — Foundations (unblock everything)

| ID        | Sev | Gap (what is wrong)                                                                                                                                                                                                                                                                                                                                                  | Evidence                                                      | Fix                                                                                                                                                                      | Acceptance                                                                              | Depends on | Status                                                                                              |
| --------- | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------- |
| **G-001** | S0  | Optional Keycloak slim PR not merged; identity/invite/OTP stores in-memory                                                                                                                                                                                                                                                                                           | PR #38 CI 13/13 green, still draft                            | Merge PR #38; follow-up: persist identity/invite/OTP                                                                                                                     | `main` has env-gated Keycloak; docs list env vars                                       | —          | **DONE (PR #40; from #38)** — env-gated Keycloak + docs; residual persist stores + live IdP (G-107) |
| **G-002** | S0  | **Two schema systems**: Prisma migrations (student/institution/staff/attendance/exam/assessment) vs raw SQL `db/sql/001–014` (health, timetable, gradebook, notifications, transport, comms, hostel, library, parent, fees, HR, admissions). CI runs `prisma:migrate:deploy` only (`ci.yml:502`), so raw-SQL modules fall back to in-memory during integration tests | `domain-plugins.ts` header; `ci.yml` 495–512; `db/sql/`       | Add `tools/scripts/apply-sql.sh` (idempotent, ordered 001→014 + seeds) and call it in CI after Prisma; document canonical order in `db/README.md`                        | Integration job applies both; a test proves `pg` repos are used when `DATABASE_URL` set | —          | **DONE (apply-sql.sh + CI)**                                                                        |
| **G-003** | S0  | No **mount matrix**: which package routes are live on the gateway is only visible by reading `domain-plugins.ts`. Fees, billing, audit, policy, tenant-lifecycle, custom-field, survey, dashboards, ETL, report, workflow (real), plugin, theme are **not** mounted                                                                                                  | `domain-plugins.ts` DOMAIN_REGISTRARS vs `packages/backend/*` | Publish `docs/audits/GATEWAY_MOUNT_MATRIX.md` (package → prefix → persistence → RBAC → E2E) and a unit test that fails when a package exports a plugin not in the matrix | Matrix committed; test green                                                            | —          | **CLOSED** (`GATEWAY_MOUNT_MATRIX.md` + `mount-matrix.ts` + `gateway-mount-matrix.test.ts`)         |

### Wave 1 — Security spine (S0)

| ID        | Sev | Gap                                                                                                                                                                | Evidence                                                               | Fix                                                                                                                          | Acceptance                                                                                   | Depends on                                    | Status                              |
| --------- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------- | ----------------------------------- | ------------------------------ |
| **G-101** | S0  | `rbacPlugin` (`packages/backend/auth/src/rbac-plugin.ts`) is **not registered** anywhere in `apps/api-gateway` → every domain route relies only on "has valid JWT" | grep `rbacPlugin` in `apps/api-gateway/src` = 0 hits; `app.ts` 239–350 | Register `rbacPlugin` after `tenantPlugin`; define permission map per prefix; default-deny for mutating verbs                | Every `POST/PUT/PATCH/DELETE` under `/api/v1/*` requires a permission; deny tests per module | G-003                                         | **DONE (rbacPlugin mounted)**       |
| **G-102** | S0  | **Forgeable actor**: parent-portal and HR leave read `request.headers['x-user-id']`; timetable/gradebook/registration read actor-ish headers                       | `parent-portal/src/routes.ts:47`, `staff/src/leave-routes.ts:29`       | Actor = JWT `sub`/claims only; strip client `x-user-id`/`x-actor` at gateway; shared `getActor(request)` helper              | Property test: header override never changes actor; all routes use helper                    | G-101                                         | **DONE (JWT-only getActor)**        |
| **G-103** | S0  | **Zero RLS** on raw-SQL tables (`db/sql/001–014`): `ENABLE ROW LEVEL SECURITY` count in `db/` = 0; RLS exists only in Prisma migrations                            | grep result                                                            | Add `db/sql/015_rls_policies.sql` (tenant_id policy on every table + `SET app.tenant_id`), wire in pg repos via `withTenant` | Tenant-isolation release gate extended to raw-SQL domains and green                          | G-002                                         | **DONE (015_rls_policies.sql)**     |
| **G-104** | S0  | Platform-admin UI plugin (`platform-admin-ui-plugin.ts`, 457 lines) serves `/tenants`, `/plans`, `/break-glass`, `/audit`, `/platform` with **no role check**      | grep `requireRole                                                      | roles` = 0                                                                                                                   | Require `platform_admin` role via G-101; break-glass needs dual-control + audit              | Non-admin → 403; audit row per admin mutation | G-101                               | **DONE (platform-admin gate)** |
| **G-105** | S1  | Audit package exists but no audit middleware mounted; admin & PHI/money mutations unaudited                                                                        | `packages/backend/audit` not in `domain-plugins.ts`                    | Mount audit plugin; emit on every mutating route (actor, tenant, resource, before/after hash)                                | Audit rows queryable; test per module                                                        | G-101                                         | **DONE (auditPlugin + onResponse)** |
| **G-106** | S1  | Tenant lifecycle / policy / billing-entitlement plugins unmounted → no plan limits, no suspend/offboard                                                            | `packages/backend/{tenant,policy,billing}` unmounted                   | Mount tenant lifecycle + entitlement middleware; wire plan checks                                                            | Suspended tenant → 403; entitlement test                                                     | G-101, G-003                                  | **DONE (suspended-tenant gate)**    |
| **G-107** | S1  | Live IdP login not proven; HS-JWT default                                                                                                                          | PR #38 scope                                                           | After G-001: staging Keycloak realm + recorded login E2E                                                                     | Evidence recording or **WAIVED (dated)**                                                     | G-001                                         | WAIVED until secrets                |

### Wave 2 — Persistence, money, PHI (S0/S1)

| ID        | Sev | Gap                                                                                                                           | Evidence                         | Fix                                                                                                             | Acceptance                                                | Depends on          | Status                                                                              |
| --------- | --- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------- |
| **G-201** | S0  | **Fees/finance has no gateway module**: `011_fees_finance_schema.sql` exists but no `feesPlugin`; only parent-portal reads it | grep `fees` in gateway = 0       | Build `packages/backend/fees` (invoices, ledger, receipts, concessions, dunning) on raw pg + RLS; mount `/fees` | Double-entry invariant tests; RBAC; restart-safe          | G-002, G-101, G-103 | **DONE** (`@proctira/backend-fees` + `/fees` mount; G-202 sandbox only)             |
| **G-202** | S0  | Payment gateway (PSP) integration absent                                                                                      | —                                | Adapter interface + sandbox stub + webhook idempotency                                                          | Sandbox charge proof or **WAIVED (dated)**                | G-201               | WAIVED until PSP keys                                                               |
| **G-203** | S0  | Health **special-needs in-memory**; PHI read access unaudited; no field-level encryption policy                               | `domain-plugins.ts` 208–223      | Persist special-needs (SQL 012 ext.); PHI access log; encrypt sensitive columns (pgcrypto or app-level)         | No plaintext PHI in logs; audit on read; tenant deny test | G-103, G-105        | **DONE** (`017` + `PgSpecialNeedsStore` + `phi-crypto` + `logPhiAccess`)            |
| **G-204** | S1  | Scholarships **in-memory + demo seed**                                                                                        | `InMemoryScholarshipRepository`  | `db/sql/016_scholarships_schema.sql` + pg repo (programs, applications, disbursements)                          | CRUD survives restart; RLS; E2E `10-scholarships` live    | G-002, G-103        | **CLOSED** (`016` + `PgScholarshipRepository` + factory)                            |
| **G-205** | S1  | Admissions CRM **in-memory**; SQL 014 unused; OCR waived                                                                      | `InMemoryRegistrationRepository` | pg repo on 014; pipeline stages; document upload stub                                                           | Persisted pipeline; RBAC; E2E                             | G-002, G-103        | **CLOSED** (`PgRegistrationRepository` + factory; OCR waived)                       |
| **G-206** | S1  | HR leave: persisted only when `DATABASE_URL`; approval actor forgeable; no payroll hooks                                      | `staff/src/leave-routes.ts`      | After G-102: approver from JWT; balances; audit                                                                 | Approve/deny matrix tests                                 | G-102               | **DONE** (`018` balances + JWT actor + approve deduct tests)                        |
| **G-207** | S1  | Notifications delivery records in-memory; no provider (email/SMS/push) adapters live                                          | `createNotificationStack`        | Persist deliveries; adapter interface; Twilio/SES stubs                                                         | Restart-safe; provider proof or **WAIVED**                | G-002               | **DONE** (PG deliveries; provider **WAIVED**)                                       |
| **G-208** | S1  | Workflow engine served by **UI seed plugin**, real `@proctira/backend-workflow` unmounted                                     | `workflowUiPlugin` comment       | Mount real workflow package with adapter matching UI shapes                                                     | Approvals persist; E2E `12-workflows` live                | G-002               | **DONE** (minimal: `019` + PG UI store; engine still unmounted)                     |
| **G-209** | S2  | Insights/platform-admin aggregates in-process, not from warehouse                                                             | `insightsUiPlugin`               | Wire `data-warehouse`/`report` packages; scheduled ETL job                                                      | Reports from DB; job runs in CI smoke                     | G-002               | **DONE** (`020` + `PgInsightsUiStore`; full report/DW/ETL packages still unmounted) |
| **G-210** | S2  | Assessment report-card repositories not wired; routes disabled                                                                | `domain-plugins.ts` 159–161      | Prisma or pg repo; enable routes or formally redirect to gradebook                                              | Report card generated E2E                                 | G-002               | **DONE** (in-memory factories wired; durable HTML also `/gradebook/report-cards`)   |

### Wave 3 — SIS depth & portals (S1)

| ID        | Sev | Gap                                                                                  | Evidence                     | Fix                                                                 | Acceptance                           | Depends on   | Status                                                                                                                                                                                |
| --------- | --- | ------------------------------------------------------------------------------------ | ---------------------------- | ------------------------------------------------------------------- | ------------------------------------ | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **G-301** | S1  | Route-permission coupling test covers core SIS only (`09-route-permission-coupling`) | e2e list                     | Extend to timetable, gradebook, health, fees, scholarships, portals | Spec asserts 403 per role per module | G-101        | **DONE** — gateway `rbac-enforcement` campus deny matrix + e2e route list extended (live personas residual)                                                                           |
| **G-302** | S1  | UUIDs shown as primary labels in timetable/gradebook/master-schedule UIs             | UX explore                   | Human labels (code + name), search-select components                | No raw UUID visible on list/detail   | —            | **DONE** — `entity-label` + `EntitySearchSelect`; timetable/schedule/gradebook lists + forms                                                                                          |
| **G-303** | S1  | Gradebook publish/lock/moderation workflow & transcript signing incomplete           | DEV_SIS_GRADEBOOK audit      | Publish states, lock after moderation, signed PDF                   | State machine tests; export job      | G-101        | **DONE** — DRAFT→SUBMITTED→APPROVED→LOCKED + HMAC transcript signature stub (not CA-sealed PDF)                                                                                       |
| **G-304** | S1  | Master schedule conflict resolution & bulk ops thin                                  | DEV_SIS_MASTER_SCHEDULE      | Conflict engine surfaced in UI; bulk assign                         | Property tests on conflicts          | G-302        | **DONE** — `GET /timetable/conflicts` + bulk enroll API/UI; clash property tests; residual student-overload enroll gate                                                               |
| **G-305** | S1  | Board exports: async job, download, retention, tenant check                          | DEV_SIS_BOARD_EXPORTS        | Job queue + signed URL + audit                                      | E2E download; deny cross-tenant      | G-105        | **DONE** — async QUEUED + process; signed download token; tenant-scoped download + audit (Playwright residual)                                                                        |
| **G-306** | S1  | Parent/student portals: consent, messaging, fees against live API; actor from header | `22-parent-portal-smoke`     | After G-102/G-201: live journeys                                    | Authenticated E2E evidence           | G-102, G-201 | **DONE** — JWT-only link actor; forge-header deny; live smoke in G-401 harness; student JWT scaffolding. Residual: dedicated student shell UI                                         |
| **G-307** | S2  | Bulk import (`05-bulk-import`) validation/rollback semantics                         | e2e                          | Dry-run, row errors, transactional commit                           | Property tests                       | G-002        | **DONE** — `dryRun`/`mode=validate` skips writes; row errors preserved; commit returns `transactional: true`. Residual: true DB transaction rollback needs transactional student repo |
| **G-308** | S2  | LMS / LTI integration only planned                                                   | `docs/plans/LMS_LTI_EPIC.md` | LTI 1.3 tool launch stub                                            | Launch proof or **WAIVED**           | —            | WAIVED until sandbox                                                                                                                                                                  |

### Wave 4 — UX, accessibility, mobile, E2E CI (S1/S2)

| ID        | Sev | Gap                                                                                                 | Evidence                                                                             | Fix                                                                                          | Acceptance                                 | Depends on | Status                                                                                                                                                                                                           |
| --------- | --- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- | ------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **G-401** | S1  | `E2E_BACKEND_READY` appears in **no** workflow → write-path E2E never runs in CI                    | grep `.github` = 0                                                                   | Add `e2e-backend-ready` job: Postgres + Redis + gateway + web, seeded admin, run write specs | Job in CI (required or nightly with badge) | G-002      | **DONE** — nightly/`workflow_dispatch` job + harness; migrate+SQL hard-fail; HS256 health write + 17c a11y smokes. Residual: seeded `loginAsTenantAdmin` journeys / IdP                                          |
| **G-402** | S1  | Authenticated axe / dark / touch / RTL specs `test.skip` when routes not enabled or backend missing | `a11y-axe.spec.ts:75,88,106,180`; `dark-mode-parity:118,186`; `touch-target:258,332` | Run in G-401 job with all module routes enabled; fail on critical violations                 | 0 critical axe across module route list    | G-401      | **DONE** — health+scholarships on axe list; HS256 `setupGatewayTenantSession`; `17c` in G-401. Residual: dark/touch/RTL full matrix + 0-critical proof on every route                                            |
| **G-403** | S1  | No visual-regression CI (skill notes "No visual-diff CI")                                           | skill map                                                                            | Playwright `toHaveScreenshot` baseline for desktop/tablet/mobile top screens                 | Diff job green; baselines committed        | G-401      | **DONE** — desktop Chromium baselines (login + parent/institutions→login) + `visual-regression.yml`. Residual: tablet/mobile matrix                                                                              |
| **G-404** | S2  | MobileShell routes to missing/empty destinations                                                    | UX explore                                                                           | Route audit; hide/404-guard; capture PNGs                                                    | All shell links resolve                    | —          | **DONE** — App Router hrefs (`/`, `/attendance`, `/students`, `/admin/users`, `/admin`, `/reports`, `/help`); dead `/app/*` removed; route audit unit tests. Residual: dedicated `/me/profile` page PNG captures |
| **G-405** | S2  | i18n parity: hardcoded strings in redesign modules; RTL only spec-level                             | `apps/web/src/i18n`                                                                  | Extraction lint rule; fill locales for top modules                                           | Lint gate; RTL spec passes for modules     | —          | **DONE** — campus keys in all locales + `campus-i18n-parity.test.ts` + `tools/scripts/check-campus-i18n.mjs`. Residual: full hardcoded-string extraction across redesign screens; RTL matrix still thin          |
| **G-406** | S2  | Flat IA for large modules; no command-palette deep links for campus modules                         | product-ia skill                                                                     | Nav grouping per IA skill; breadcrumbs; palette entries                                      | UX review checklist filled                 | —          | **DONE** — Campus group in CommandPalette (fees/hostel/transport/library/communication/admissions) + App Router paths; sidebar already grouped. Residual: formal UX checklist sign-off                           |
| **G-407** | S2  | Flutter app: a11y semantics, offline, and device-farm evidence thin                                 | mobile explore                                                                       | Semantics pass; golden tests; farm run                                                       | Checklist + PNGs or **WAIVED** farm        | —          | **DONE** — goldens + offline IT + `MOBILE_FLUTTER_ENTERPRISE.md`; **device-farm WAIVED** (no emulator/SDK in CI)                                                                                                 |
| **G-408** | S2  | Multi-board 3×2×500 live certification not a standing proof                                         | skill §Live DB                                                                       | Add to G-401 job or nightly                                                                  | `verify-counts.txt` artifact refreshed     | G-002      | **DONE** — `e2e-backend-ready.yml` multi-board step + artifact upload; `setup-live-db-and-onboard.sh` ARTIFACT_DIR-aware. Residual: first nightly artifact refresh on Actions                                    |

### Wave 5 — Platform, release, operations (S1/S2)

| ID        | Sev | Gap                                                                                                                | Evidence                     | Fix                                                                             | Acceptance                                    | Depends on | Status                                                                                                                                                                                          |
| --------- | --- | ------------------------------------------------------------------------------------------------------------------ | ---------------------------- | ------------------------------------------------------------------------------- | --------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **G-501** | S1  | Helm chart is `infrastructure/helm/openemis-platform`; deploy.yml assumes per-service charts `proctira-${SERVICE}` | `deploy.yml:270`; Chart.yaml | Rename/restructure charts per app; values per env; secrets via external-secrets | `helm template` in CI; staging deploy dry-run | —          | **DONE** — dir → `proctira-platform`; new `proctira-service` chart matching deploy.yml; `helm-template.yml` CI. Residual: external-secrets                                                      |
| **G-502** | S1  | Observability: Prometheus/Grafana/alerts in `infra/observability` but no SLOs per module, no runbooks link         | dir listing                  | SLO doc + alert rules per critical journey; runbooks                            | Alerts fire in compose test                   | —          | **DONE** — `docs/observability/SLO_CRITICAL_JOURNEYS.md` + `critical_journeys.yml`. Residual: live compose fire proof / route labels                                                            |
| **G-503** | S1  | Backup/restore, DR, data-retention (PHI/minor data) policies absent                                                | —                            | Documented + scripted pg backup/restore; retention jobs                         | Restore drill evidence                        | G-002      | **DONE** — `pg-backup.sh` / `pg-restore.sh` / `restore-drill.sh` + `phi-retention-job.mjs` + `docs/DATA_RETENTION.md` / BACKUP_RESTORE §11. Residual: scheduled cron in prod clusters           |
| **G-504** | S2  | Secrets management: env files; no rotation story for JWT secret / DB creds                                         | `config.ts`                  | Document rotation; support dual JWT keys (kid)                                  | Rotation test                                 | G-001      | **DONE** — `JWT_SECRET_PREVIOUS` dual-verify + `docs/SECRETS_ROTATION.md` + rotation tests. Residual: live Keycloak/JWKS rotation drill when IdP secrets available (G-107)                      |
| **G-505** | S2  | Rate-limit & idempotency exist, but no per-tenant quotas / abuse detection                                         | `plugins/rate-limit.ts`      | Tenant-scoped limits from plan (G-106)                                          | Test 429 per tenant                           | G-106      | **DONE** — plan-tier `max` via `tenant-plan-quotas.ts` + free-vs-enterprise 429 test                                                                                                            |
| **G-506** | S2  | Statuspage / incident comms / pager absent                                                                         | —                            | Integration stub                                                                | **WAIVED (dated)**                            | —          | WAIVED                                                                                                                                                                                          |
| **G-507** | S2  | Install wizard / onboarding of new tenant end-to-end not certified                                                 | `apps/install-wizard`        | E2E: create tenant → admin → school → first student                             | Recording                                     | G-106      | **DONE** — `e2e/02-tenant-onboarding-harness.spec.ts` + `tools/scripts/install-wizard-onboarding-harness.sh` (RECORD=1 video). Residual: live upstream install API / IdP waived without secrets |
| **G-508** | S3  | Supply-chain: no SBOM / dependency audit gate / image signing                                                      | workflows                    | `pnpm audit` gate, SBOM, cosign                                                 | CI job green                                  | —          | **DONE** — `supply-chain.yml` (pnpm audit artifact + CycloneDX stub + cosign digest stub). Residual: live cosign sign needs registry creds; audit may report known vulns                        |

### Wave 6 — Long-tail campus modules (after G-101/G-103)

| ID        | Sev | Module                                                | Required to close                                      | Depends on   | Status                                                                                                                           |
| --------- | --- | ----------------------------------------------------- | ------------------------------------------------------ | ------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| **G-601** | S2  | Hostel                                                | RBAC + RLS + live E2E write + audit md                 | G-101, G-103 | **DONE** — RBAC deny matrix + `015` RLS + `21d` write smoke + HOSTEL audits. Residual: mess fee ledger                           |
| **G-602** | S2  | Transport                                             | same + GPS/attendance-on-bus stub                      | G-101, G-103 | **DONE** — RBAC deny + `015` RLS + `21d` writes + GPS/attendance-on-bus sandbox stubs. Residual: live telematics device adapter  |
| **G-603** | S2  | Library                                               | same + fines → fees ledger                             | G-201        | **DONE** — fines `/library/fines/assess` → shared FeesService invoice; RBAC/RLS/`21d` smoke. Residual: overdue auto-assess job   |
| **G-604** | S2  | Communication / emergency                             | RBAC (no send without role) + provider adapter + audit | G-101, G-207 | **DONE** — teacher/parent send deny; sandbox delivery adapter; local + gateway audit on send/dispatch. Residual: live Twilio/SES |
| **G-605** | S2  | Surveys / custom fields / dashboards / theme packages | Mount, RBAC, E2E or formally park                      | G-003        | **DONE (PARKED)** — `EXPECTED_PARKED` + `parkedReason` in mount-matrix + gateway test. Residual: mount when product UI funded    |
| **G-606** | S2  | Public website + registration portal                  | Enterprise packs (SEO, forms, a11y, spam)              | G-205        | **DONE** — `G606_PUBLIC_REGISTRATION_ENTERPRISE_PACK.md` + existing 9.5 checklists. Residual: live registration apply stack      |
| **G-607** | S2  | Developer portal / plugin marketplace                 | AuthZ, API keys, rate limits, docs                     | G-101, G-505 | **DONE** — `/developer` mounted; RBAC resource `developer`; gateway rate-limit; in-memory keys/docs. Residual: live IdP key mint |

### Wave 7 — Fresh whole-software review (2026-09-08, five-pillar code re-audit after Waves 0–6)

Found by re-reading the tree, not the register. Every row below was OPEN when written; Status is updated in the same PR (#40).

| ID        | Sev | Gap                                                                                                                                                         | Evidence                                                                                                   | Fix                                                                                                          | Acceptance                                                                     | Status |
| --------- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ | ------ |
| **G-701** | S0  | Student enrollment / transfer / bulk-import routes exported but **never registered**; web calls `/enrollments/*`, `/students/import` against dead endpoints | `student-plugin.ts` registers only `registerStudentRoutes`; `apps/web/src/lib/api/students.ts:319,354`     | Register enrollment + import routes in `studentPlugin`; gateway test                                         | 201/200 from `/api/v1/enrollments`, `/api/v1/students/import` in gateway tests | **DONE** — `studentPlugin` registers enrollment + import routes (`create-enrollment-repository.ts`, Pg repo on `021` `enrollment_history`/`transfer_records`); mount test in `gateway-mount-matrix.test.ts` |
| **G-702** | S0  | Gateway RBAC **default-allow** on unmapped mutating prefixes (`/billing`, `/tenant-lifecycle`, `/reports`, `/data-warehouse`, native assessment routes)     | `app.ts` `if (!resource) return;`; `rbac-registry.ts` map lacks those segments                             | Map every mounted prefix; unmapped `/api/v1` mutation → 403 default-deny; mount-matrix test enforces         | Test: non-admin POST `/billing/plans` → 403; unmapped segment → 403            | **DONE** — `PATH_RESOURCE_MAP` covers every mounted prefix (matrix test), `UNMAPPED_API_RESOURCE` default-deny for non-platform-admins (`rbac-enforcement.test.ts`) |
| **G-703** | S0  | Weak JWT default (`dev-secret-change-in-production`) boots in production; k8s configmap ships `CHANGE_ME`                                                   | `config.ts:160`; `infrastructure/k8s/base/configmap.yaml:36`                                               | Fail-closed when `NODE_ENV=production` and secret missing/default/short; move to Secret                      | Unit test: prod + default secret throws                                        | **DONE** — `resolveJwtSecret` throws in production on missing/weak/<32-char secret (`config.test.ts`); k8s secret moved out of base into dev `secrets.yaml` + staging/prod `ExternalSecret` |
| **G-704** | S0  | Audit, billing, tenant-lifecycle, Keycloak identity, invites, OTP, platform-admin state are **in-memory only**                                              | `app.ts:308,354,562-577`; `invite-service.ts`; `platform-admin-ui-plugin.ts:153`                           | `db/sql/021_control_plane_schema.sql` + Pg repos selected when `DATABASE_URL`                                | Restart-safe tests per store; RLS on new tables                                | **DONE** — `022_control_plane_schema.sql` (`audit_log_entries` append-only + archive, `control_plane_documents` RLS) + `PgAudit/PgBilling/PgTenant/PgUserInvite/PgOtpChallenge/PgKeycloakIdentity` stores and platform-admin `PgKeyedStore`; factories pick Pg on `DATABASE_URL`; live `*.pg` tests pass |
| **G-705** | S0  | Demo data seeded into any environment (`seedScholarshipDemoData()` on boot; `apply-sql.sh` applies `*b_seed.sql`)                                           | `domain-plugins.ts:218`; `tools/scripts/apply-sql.sh`                                                      | Gate behind `SEED_DEMO_DATA=1` / non-production; `APPLY_SEEDS=1` for seed files                              | Prod boot has zero demo rows; CI still seeds explicitly                        | **DONE** — `shouldSeedDemoData()` gate (never in production / when `DATABASE_URL` set unless `SEED_DEMO_DATA=1`); `apply-sql.sh` applies `*b_seed.sql` only with `APPLY_SEEDS=1` |
| **G-706** | S0  | No PR-level live E2E gate; nightly runs 3 specs and soft-skips if gateway down                                                                              | `e2e-backend-ready.yml` schedule-only; `run-e2e-backend-ready.sh`                                          | PR job with Postgres service, `E2E_REQUIRE_LIVE=1`, hard-fail; expand spec list                              | Job on PR, red on failure                                                      | **DONE** — `e2e-backend-ready.yml` now runs on `pull_request` with `E2E_REQUIRE_LIVE=1` (gateway must come up; any failing spec is red) over a 6-spec `PR_SPECS` set (health counselling, module a11y, parent portal, comms, insights, notification prefs); nightly runs 12 specs; dispatch defaults `require_live=true`. SQL applied with `APPLY_STRICT_FKS=1` and the harness seeds the fixed E2E tenant rows (`tools/e2e/seed-e2e-tenants.sql`, platform-admin scope tx-local) so strict FKs + FORCE RLS on `tenants` hold. Harness refuses a busy port and kills the gateway process group (stale `pnpm` children previously made local runs test the wrong code). Gate immediately caught 4 wave-7 regressions, all fixed: Pg notification prefs/devices store now binds `app.tenant_id` (`withPgTenant`) — RLS 500s; comms live-audience counts tenant-scoped; parent/guardian/student roles granted portal self-service writes (`PORTAL_SELF_SERVICE_WRITES`) — 403s after default-deny; insights live spec mints a real HS256 cookie (fail-closed JWT rejected the fake one) and waits on `data-hydrated` before driving forms. Local: 26/26 PR-set tests green |
| **G-707** | S0  | Backup / restore-drill / PHI-retention scripts exist but are **not scheduled**                                                                              | no `kind: CronJob`; no workflow schedule                                                                   | Helm CronJobs (backup, retention) + weekly restore-drill workflow                                            | `helm template` renders CronJobs; drill workflow exists                        | **DONE** — Helm `templates/dr/`: `pg-backup` CronJob (daily, PVC, `BACKUP_RETENTION_DAYS` pruning), `phi-retention` CronJob (weekly, dry-run unless `dr.phiRetention.apply`), shared PVC; both run new `infrastructure/docker/Dockerfile.dr-tools` (psql 16 + repo scripts, added to `release.yml` matrix); `helm-template.yml` asserts ≥2 CronJobs + PVC render. `restore-drill.yml` weekly + on DR-path PRs: seeded Pg 16 → dump → restore → row-parity diff (hard-fail unless `full-db`), NOBYPASSRLS refusal, prune test, PHI dry-run, image smoke. Found+fixed: FORCE RLS made `pg_dump` fail/empty for the app role — `pg-backup.sh` now preflights BYPASSRLS (exit 2), chart adds `secrets.backupDatabaseUrl` → `BACKUP_DATABASE_URL`; `docs/BACKUP_RESTORE.md` §3.4/3.5/7.1 rewritten |
| **G-708** | S0  | Supply-chain gate is soft (`pnpm audit` continue-on-error; SBOM/cosign stubs; docs overclaim)                                                               | `supply-chain.yml`; `docs/SUPPLY_CHAIN.md`                                                                 | Fail on high/critical (allowlist); real CycloneDX SBOM; cosign gated on registry creds, docs honest          | Workflow fails on high vuln; SBOM artifact real                                | **DONE** — `supply-chain.yml` now runs on PR/main/nightly: `tools/scripts/audit-gate.mjs` hard-fails on unwaived high/critical (`tools/supply-chain/audit-allowlist.json` entries need reason + `trackedBy` gap + `expires`; expired waivers fail; `node --test` self-check); 44 → 11 high advisories after `pnpm.overrides` (fast-jwt 6.2.4, brace-expansion, fast-uri, nanoid, postcss, tmp, browserslist) + bumps (react-router-dom 7.18.2, faker 10.5); remaining 11 are Next 15 / Fastify 5 majors, waived until 2026-12-31 under G-735; real CycloneDX 1.5 SBOM via cdxgen (1,322 purl'd components, shape-validated); keyless cosign sign + SBOM attest + verify of the api-gateway image gated on `SUPPLY_CHAIN_SIGN_IMAGES` with an explicit `signing-skipped` summary; `docs/SUPPLY_CHAIN.md` rewritten honestly |
| **G-709** | S0  | Live PSP / email-SMS-push / IdP / LMS providers absent (sandbox or waived)                                                                                  | `payment-adapter.ts`; `sandbox-*`; no LTI pkg                                                              | Adapters exist; needs secrets                                                                                | **WAIVED (dated)** until owner supplies keys                                   | WAIVED |
| **G-710** | S1  | RLS: no `FORCE ROW LEVEL SECURITY`; 4 tables lack RLS; 9 pg repos never `set_config`/`withPgTenant` -> **Done:** `021_wave7_integrity_schema.sql` FORCEs RLS on every `tenant_id` table and adds policies for `tenant_theme_versions/drafts`, `insights_ui_runs`, `import_jobs`; hostel/gradebook/health(phi,counselling)/communication/library/transport/timetable/parent-portal Pg repos route every query through `withPgTenant` (transaction-local `app.tenant_id`). | `db/sql/*`; transport/timetable/hostel/library/communication/gradebook/parent-portal/counselling/phi repos | `021` adds FORCE + missing policies; repos wrap queries in `withPgTenant`                                    | `hostel/pg-hostel-repository.test.ts` cross-tenant + unscoped reads = 0 rows; `rg 'pool.query' packages/backend/*/src/pg-*.ts` outside `withPgTenant` = 0 | DONE   |
| **G-711** | S1  | PHI plaintext: `phi-crypto` no-ops without key; counselling/screening notes never encrypted -> **Done:** `phi-crypto` throws `PhiKeyMissingError` in production without `PHI_ENCRYPTION_KEY` (`assertPhiKeyConfigured()` boot guard in gateway health mount; `ALLOW_PLAINTEXT_PHI=1` explicit opt-out); `PgCounsellingStore` encrypts reason/case_notes/outcome, `PgPhiStore` encrypts measurement/condition/vaccination/insurance notes, allergy + medication + policy_number. | `phi-crypto.ts:12-14`; `pg-counselling-store.ts:83`; `pg-phi-store.ts:227`                                 | Require key in production; encrypt notes columns                                                             | `phi-crypto.test.ts` prod-without-key throws; `health/pg-counselling-store.test.ts` asserts `enc:v1` ciphertext at rest + transparent decrypt | DONE   |
| **G-712** | S1  | RBAC only on mutating verbs — campus GETs are JWT-only                                                                                                      | `app.ts` `MUTATING_METHODS`                                                                                | Evaluate `read` permission on GET for mapped resources                                                       | Test: role without read → 403 on GET                                           | **DONE** — `onRequest` RBAC enforces `read` on GET for every `/api/v1` resource; role matrix extended (`student` role, `SELF_SERVICE_READ_RESOURCES`) |
| **G-713** | S1  | Unauthenticated exposure: `/api/v1/services` leaks upstream URLs; `/api/v1/auth/roles` public; `/docs` public                                               | `app.ts` excludes; `service-router.ts:151`                                                                 | Require auth for services + roles; `/docs` off in production unless `DOCS_PUBLIC=1`                          | 401 without token                                                              | **DONE** — `/api/v1/services` + `/api/v1/auth/roles` require JWT; service targets hidden unless platform_admin; `/docs` only outside production or with `DOCS_PUBLIC=1` |
| **G-714** | S1  | Silent in-memory fallback in every repository factory when `DATABASE_URL` absent                                                                            | `create-*-repository.ts`, `repository-factory.ts`                                                          | Shared `persistenceMode()` helper: warn once; `REQUIRE_DATABASE=1`/production → throw                        | Test: production without DB throws                                             | **DONE** — `assertInMemoryFallbackAllowed()` in every repository factory: throws in production / `REQUIRE_DATABASE=1`, warns once in dev |
| **G-715** | S1  | Real workflow engine / report / DW / ETL packages unmounted; UI served by gateway seed plugins -> **Done (engine):** `@proctira/backend-workflow` mounted under `/workflow-engine` (registrar `workflow-engine`) with `PgWorkflowRepository`/`PgCaseRepository` on new `db/sql/025_workflow_engine_schema.sql` (definitions, instances, append-only transition audit, cases; RLS+FORCE); `createWorkflowRepositories()` picks Pg when `DATABASE_URL`. Report / DW / ETL remain honestly PARKED via insights-ui (G-209). | `domain-plugins.ts`; `EXPECTED_UNMOUNTED`                                                                  | Mount `@proctira/backend-workflow` engine under `/workflow-engine`; keep report/DW/ETL honestly PARKED       | `api-gateway/workflow-engine-mount.test.ts` (201/200 through gateway + RBAC 401/403); `workflow/pg-workflow-repository.live.test.ts` (durability across plugin restarts, RLS isolation, audit immutability) | DONE   |
| **G-716** | S1  | Report cards / transcripts are HTML "pdf-lite"; no `pdfGenerator` wired                                                                                     | `transcript-artifact.ts`; `assessment-plugin.ts:55`                                                        | **Done:** new `@proctira/pdf-lite` (dependency-free PDF 1.4 writer: Catalog/Pages/Helvetica, xref, `PdfFlow` layout w/ tables + page breaks; `inspectPdf` verifier). Assessment: `ReportCardPdfGenerator` default in plugin, `ReportCardArtifactStore` (fs/memory) keeps bytes, inline processing when no queue, `assembleStudentResults` now real (new `findByStudentPeriod` on result repo), `POST /report-cards/jobs/:id/process`, `GET /report-cards/jobs/:id/download` → `application/pdf`. Gradebook: `transcript.pdf` written + is `artifactUri` (`artifactKind: 'pdf'`), `GET /gradebook/transcripts/:id/download?format=pdf|html|json`. Examination `SimplePdfGenerator` (admit cards/seating/certificates) rewritten on pdf-lite | `pdf-lite` 7 tests (xref offsets verified; pypdf parses output), `report-card-pdf.test.ts` 8, gradebook transcript test asserts `%PDF-1.4`, `pdf-generator.test.ts` 3 | DONE   |
| **G-717** | S1  | Memory-only: assessment report-card repos, staff appraisal/training (routes unregistered), admissions CRM waitlist/interviews (SQL 014 unused) -> **Done:** SQL `024_wave7_domain_persistence_schema.sql` (hr_appraisal_*, hr_training_*, hr_certifications, report_card_* with RLS+FORCE+updated_at; RLS on admission_waitlist/interview tables). Staff: `PgAppraisal*Repository`, `PgTraining*Repository`, `PgCertificationRepository` + `createAppraisalRepositories`/`createTrainingRepositories`; `staffPlugin` now registers `appraisalRoutes` + `trainingRoutes` (`staffAppraisalService`/`staffTrainingService`). Registration: `AdmissionsCrmStore` interface, `PgAdmissionsCrmStore` on 014 (advisory-locked waitlist positions, `FOR UPDATE` slot bookings, idempotent conflicts), `createAdmissionsCrmStore()` wired in gateway; tracking/id lookups + `updateStatus` now bind tenant GUC (FORCE RLS made the public status check return 404 on Pg). Assessment: `PgReportCardTemplate/TeacherComment/InstitutionBranding/ReportCardJobRepository` on 024; factories choose Pg when `DATABASE_URL` | `assessment/repository-factory.ts`; `staff-plugin.ts`; `registration/pg-admissions-crm-store.ts`; `assessment/pg-report-card-repository.ts` | Register appraisal/training routes; Pg CRM store on `014`; Pg report-card stores on `021` | `staff/hr-pg-and-routes.live.test.ts` (routes 200 + RLS), `registration/pg-admissions-crm-store.test.ts`, `assessment/pg-report-card-repository.live.test.ts` | DONE   |
| **G-718** | S1  | Integrity: fees amount-equality not double-entry; leave check-then-adjust race; no `updated_at` triggers; ~50 tables lack `tenant_id` FK; no SQL ledger -> **Done:** `023_fee_ledger_schema.sql` double-entry `fee_ledger_entries` (append-only trigger, DEFERRABLE balance check per journal, RLS+FORCE); `FeesService` posts DR AR/CR revenue on issue, DR cash/CR AR on payment, reversal on void (`GET /fees/invoices/:id/ledger`, `/fees/ledger/trial-balance`); leave `adjustBalance` locks `FOR UPDATE` and throws `InsufficientLeaveBalanceError`; `021` adds `proctira_set_updated_at()` triggers + `schema_migrations` ledger; `021b` opt-in tenant FKs (`APPLY_STRICT_FKS=1`). | `fees-service.ts:253`; `leave-service.ts:96`; `db/sql/*`                                                   | `021` ledger table + trigger fn + FKs + `schema_migrations` ledger; leave `FOR UPDATE`                       | `fees/pg-fees-ledger.live.test.ts` unbalanced journal rejected at COMMIT + append-only; `staff/pg-leave-repository.live.test.ts` 5 concurrent approvals on 3-day balance → exactly 1 succeeds | DONE   |
| **G-719** | S1  | Web: no CSRF token; cookie `secure` only in production; developer-portal trusts `x-account-id`                                                              | `cookies.ts:26-37`; `developer-portal/.../routes.ts:1200`                                                  | **Done:** `apps/web/src/lib/auth/csrf.ts` (origin/Sec-Fetch-Site check + double-submit `csrf_token`/`x-csrf-token`, Edge-safe); middleware gates unsafe `/api/*` (403 `CSRF_REJECTED`) and issues the cookie on page navigations; internal refresh mints its own pair; browser helpers (`session.ts`, `api/auth.ts`, `browser-gateway.ts`) send the header; gateway CORS allows `X-CSRF-Token`. `isSecureCookieContext()` sets `Secure` in prod, `COOKIE_SECURE=1`, https app URL, https request or `x-forwarded-proto: https`. Developer-portal rating/review use `request.user.sub` only, 401 when anonymous; `x-account-id` ignored | `csrf.test.ts` (17), `cookies.test.ts` (6), `middleware.test.ts` CSRF gate (6), `routes-identity.test.ts` (3) | DONE   |
| **G-720** | S1  | Prisma tenant GUC set via `$executeRawUnsafe` string interpolation                                                                                          | `packages/shared/tenant/src/fastify-plugin.ts:146-148`                                                     | `SELECT set_config('app.tenant_id', $1, true)` parameterized                                                 | Unit test with quote in id                                                     | **DONE** — `set_config(app.current_tenant_id, $1, true)` bound parameter in `@proctira/tenant`; injection test in `fastify-plugin.test.ts` |
| **G-721** | S1  | Redesign modules: 0 `useTranslations`; hardcoded English; `he` in rtlLocales without `he.json`                                                              | `(dashboard)/{health,fees,hostel,transport,library,scholarships}/page.tsx`; `i18n/config.ts`               | Extract strings to `messages/*`; parity gate covers all redesign namespaces; fix `he`                        | Parity test green; zero literal headings in listed pages                       | OPEN   |
| **G-722** | S1  | Authenticated axe/dark/touch/RTL skip on PR; axe list omits fees/admissions/timetable/gradebook/attendance/workflows                                        | `a11y-axe.spec.ts:105-160`                                                                                 | Extend axe list; run `17c` + axe in G-706 PR job                                                             | PR job runs authenticated axe                                                  | OPEN   |
| **G-723** | S1  | Visual regression = 3 desktop login shells; no tablet/mobile baselines                                                                                      | `24-visual-regression.spec.ts`                                                                             | Add iPad + Pixel 5 projects and baselines                                                                    | Baselines committed; job green                                                 | OPEN   |
| **G-724** | S1  | Deploy uses thin `proctira-service` (no HPA/PDB/NetworkPolicy/securityContext); no ExternalSecret; image matrix omits 4 apps                                | `deploy.yml`; `infrastructure/helm/proctira-service`                                                       | Add templates (HPA, PDB, NetworkPolicy, securityContext, ExternalSecret opt-in); extend SERVICE_MAP          | `helm template` renders; CI green                                              | **DONE** — `proctira-service` chart: non-root/read-only-rootfs pod + container `securityContext` (uid 1001, drop ALL, RuntimeDefault seccomp, `/tmp` emptyDir), ServiceAccount (no token automount), `PORT` injected, startup probe, `serviceProfiles` map probe path (`/api/health` for Next apps — previously every Next deployment probed `/health` on the wrong port), scrape annotations only for Fastify services; new templates `hpa.yaml` (autoscaling/v2), `pdb.yaml`, `networkpolicy.yaml` (same-ns + named ingress/monitoring namespaces + DNS + HTTPS egress), `externalsecret.yaml` (ESO opt-in, `envFrom` target), topologySpreadConstraints; staging enables PDB+NetworkPolicy, production adds HPA 3–12 + zone/host spread. `release.yml` matrix adds admin-console/developer-portal/install-wizard/public-website via `Dockerfile.nextjs-app` with per-app ports. `helm-template.yml` asserts securityContext, SA, PDB, NetworkPolicy, probe path per service, and HPA + ExternalSecret on the production profile |
| **G-725** | S1  | Observability: no OTEL exporter despite docs ✅; alert receivers `REPLACE_ME`; Prometheus scrapes unregistered services                                     | `packages/shared/observability`; `infra/observability/*`; `docs/PRODUCTION_READINESS.md`                   | Docs honest (metrics-only); alertmanager receivers via env templating; register plugin in standalone servers | Doc + config updated                                                           | **DONE** — `@proctira/observability` plugin registered in all 9 standalone backend servers (gateway + etl-worker already had it) with an `enabled` switch (`METRICS_ENABLED`, default on); `prometheus.yml` scrapes only the 11 processes that actually expose `/metrics` (was 20+ phantom targets); `alertmanager.yml` is now `alertmanager.yml.tpl` rendered by `render-alertmanager.sh` (`envsubst`, POSIX sh) — `ALERT_EMAIL_TO`/`ALERT_SMTP_*`/`PAGERDUTY_INTEGRATION_KEY`/`SLACK_*_WEBHOOK_URL`; missing optional keys fall back to email so no severity routes to a dead receiver, rendered file gitignored and rendered by a compose init container; `observability-config.yml` CI runs `promtool check config/rules`, `amtool check-config` on both minimal and full renders, asserts no phantom targets and that the rendered file is never tracked; `install-cli readiness` scores metrics + alerting only and states tracing is not implemented (`TRACING_ENABLED`/`OTEL_*` no longer count); `PRODUCTION_READINESS.md` §6 and `.env.example` rewritten to match |
| **G-726** | S1  | Governance: no CODEOWNERS / Dependabot / CHANGELOG / migration-rollback runbooks                                                                            | `.github/`                                                                                                 | Add all four                                                                                                 | Files present                                                                  | **DONE** — `.github/CODEOWNERS` (platform/security, data layer, PHI domains, CI/infra, product surfaces, governance docs; owner fallback until org teams exist); `.github/dependabot.yml` (npm workspace grouped minor/patch + fastify/next/prisma groups, majors for G-735 frameworks excluded from unattended PRs; github-actions, docker, pub); `CHANGELOG.md` (Keep-a-Changelog, Unreleased populated from wave 7, 0.1.0 baseline); `docs/runbooks/database-migration-rollback.md` (two schema tracks, pre-flight backup with BYPASSRLS role, apply order, forward-revert vs `prisma migrate resolve` vs point-in-time restore decision tree, post-rollback verification) indexed in runbooks README |
| **G-727** | S1  | Web UI missing for billing, audit-logs, tenant-lifecycle; no `not-found.tsx`; `/help` stub; MobileShell omits campus modules                                | `apps/web/src/app`; `mobile-shell-routes.ts`                                                               | Add pages (App Router) against gateway APIs; `not-found.tsx`; help content; campus in MobileShell            | Routes render; route-audit test                                                | **DONE** — `/billing` (plan catalogue: tier/status/search filters, quota matrix, enabled features), `/audit-logs` (entity/actor/operation/date filters in the URL, changed-field diff, paginated) and `/tenant-lifecycle` (status/region/search, suspension reason, retention date) are server-rendered against `GET /api/v1/{billing/plans,audit-logs,tenant-lifecycle}` via `lib/api/platform.server.ts`; 403 renders an explicit "platform administrator required" notice (`platform-forbidden-notice`) distinct from the scaffold banner, gateway errors show the code. Admin hub gains a Platform card group (`admin-sections.ts`). App-level `not-found.tsx` (real 404 status). `/help` has module guide index, shortcuts, runbook index and support checklist. MobileShell drawer adds Fees/Hostel/Transport/Library/Communication. `route-audit.test.ts` globs every `page.tsx`/`route.ts` and asserts sidebar, MobileShell, admin hub, help and 404 links resolve — it immediately caught the drawer's dead `/api/auth/signout` (fixed → `/api/auth/logout`). `25-platform-surfaces-smoke.spec.ts` (PR gate): unauth redirects, 404 boundary, help content; live: platform admin sees real rows on all three surfaces after writing an audit entry + creating a tenant through the gateway, tenant admin gets the forbidden notice. `next build` renders all four routes; 11/11 live |
| **G-728** | S1  | `.env.example` missing `JWT_SECRET_PREVIOUS`, `PHI_ENCRYPTION_KEY`, `REQUIRE_DATABASE`, `SEED_DEMO_DATA`, `DOCS_PUBLIC`                                     | `.env.example`                                                                                             | Add with comments                                                                                            | Present                                                                        | **DONE** — `.env.example` documents `REQUIRE_DATABASE`, `SEED_DEMO_DATA`, `APPLY_SEEDS`, `APPLY_STRICT_FKS`, `JWT_SECRET_PREVIOUS`, `PHI_ENCRYPTION_KEY`, `DOCS_PUBLIC` |
| **G-731** | S2  | Idempotency Redis-only; rate-limit key trusts `x-tenant-id`; console SMS logs OTP; `MFA_EXPOSE_OTP`                                                         | `idempotency.ts:72`; `app.ts:159`; `sms-provider.ts:22`                                                    | In-memory idempotency fallback w/ warn; JWT tenant first; redact OTP; refuse `MFA_EXPOSE_OTP` in prod        | Tests                                                                          | **DONE** — `InMemoryIdempotencyStore` fallback (bounded, TTL) with boot warning when `REDIS_URL` unset; `rateLimitKeyFor()` buckets by JWT tenant+sub, else host tenant+IP, else IP — `x-tenant-id` header never keys or selects a plan tier; `ConsoleSmsProvider` masks phone + 4–8 digit codes (`redactOtpDigits`); gateway refuses `MFA_EXPOSE_OTP=true` under `NODE_ENV=production`. Tests: `rate-limit-key.test.ts`, `idempotency.test.ts`, `otp-service.test.ts` |
| **G-732** | S2  | Prisma migrations lack RLS on `attendance_audit`, `tenant_theme_*`, `tenants`; missing `tenant_id` indexes (4 tables); no pg tests for 5 stores             | Prisma migrations; `db/sql/*`                                                                              | `021` indexes; Prisma RLS migration; pg tests for gradebook/timetable/parent-portal/leave/UI stores          | Tests + migration                                                              | **DONE** — Prisma `20260908_wave7_rls_hardening`: FORCE RLS + policies on `attendance_audit` (tenancy derived from parent `student_attendance`/`staff_attendance`), `tenants` (self or `app.platform_admin`), `tenant_theme_versions/drafts`; `provisionTenant()` binds platform scope then the new tenant id transaction-locally; `021` §6 adds the 4 missing `tenant_id` indexes. Live pg tests: gradebook (credit rules, grading scales+bands), timetable (rooms, bell schedules/periods — soft delete now retires periods), parent-portal (links, fee plans, invoices), workflow-ui store (restart-safe definitions, atomic `decideApproval`), attendance audit RLS; insights-ui smoke pre-existing; leave concurrency pre-existing |
| **G-733** | S2  | Repo hygiene: 1,066 files Prettier drift; CI lint/format tip-only hides debt                                                                                | `pnpm exec prettier --check`                                                                               | Format repo; add repo-wide format check job                                                                  | `prettier --check` repo-wide green in CI                                       | OPEN   |
| **G-734** | S2  | Product: promotion/alumni, payroll, concessions/refunds/GL, inventory, e-sign, DSAR export, LMS, telematics, OCR absent; survey/custom-field PARKED         | capability matrix                                                                                          | Backlog rows; DSAR export endpoint on audit/PHI stores where feasible                                        | Tracked; DSAR endpoint test                                                    | OPEN   |
| **G-735** | S1  | Major framework debt: Next.js 14.2 (6 apps) and Fastify 4 (gateway + backend packages, `@fastify/static` 7, `find-my-way` 8) carry 11 high GHSAs with fixes only in Next 15 / Fastify 5; `release.yml` image matrix unsigned | `tools/supply-chain/audit-allowlist.json`; `apps/*/package.json`; `release.yml` | Upgrade Next 15 + Fastify 5 (codemods, `@fastify/jwt` 9, `@fastify/static` 8); extend keyless cosign to the release matrix | `audit-gate` passes with an empty allowlist; every released image verifiable | OPEN (waivers expire 2026-12-31) |

---

## 3. External waivers (dated 2026-09-08; re-check each release)

| Waiver              | Blocked gap | Needed from owner              |
| ------------------- | ----------- | ------------------------------ |
| Live Keycloak realm | G-107       | Staging realm + client secrets |
| Payment PSP sandbox | G-202       | Sandbox keys                   |
| Twilio / SES        | G-207       | Account + sender               |
| LMS/LTI sandbox     | G-308       | Platform registration          |
| Device farm         | G-407       | Farm account                   |
| Statuspage / pager  | G-506       | Accounts                       |

Agents implement adapters + mocks and keep the waiver row until evidence exists.

---

## 4. Execution order for Auto-mode agents

```
Serial:   G-001 → G-002 → G-003
Parallel: G-101 | G-103 | G-501 | G-404 | G-405 | G-302
Then:     G-102, G-104, G-105, G-106            (need G-101)
Then:     G-201, G-203, G-204, G-205, G-206, G-207, G-208   (need G-002/G-103)
Then:     G-401 → G-402 → G-403 ; G-408 ; G-301 ; G-303–G-307
Then:     G-502–G-508 ; G-601–G-607
```

Parallel only on **non-overlapping paths**. Suggested branch names: `cursor/g-101-rbac-mount-56c3`, `cursor/g-002-sql-apply-56c3`, etc.

---

## 5. Agent prompt (copy per gap)

```text
You are a Cursor Auto-mode subagent for ProctiraERP.
Gap: <G-xxx> from docs/audits/ENTERPRISE_FABLE51_PRODUCT_GAP_AUDIT.md
Obey: .cursor/skills/enterprise-module-production-ready/SKILL.md and the skill
named for this gap's pillar. Branch cursor/<g-xxx-short>-56c3 from origin/main.
Implement ONLY this gap. Meet the Acceptance column exactly. Add/extend tests.
Run lint + typecheck + affected unit tests. Commit, push, open draft PR.
Update the Status column of this gap in the same PR (DONE (PR #n)).
Never claim waived pillars (IdP/PSP/Twilio/LMS/Statuspage/farm) as complete.
```

---

## 6. Definition of "world-class enterprise ERP" (exit bar)

All true, waivers only where dated in §3:

1. **Security:** RBAC mounted and default-deny; JWT-only actors; RLS on every tenant table (Prisma + raw SQL); platform-admin authZ; audit on all mutations; entitlements enforced.
2. **Data:** Single documented schema-apply path used by CI, local, and certification; **no critical domain in-memory**; backup/restore drill.
3. **Function:** Fees, admissions, scholarships, HR leave, workflows, notifications persisted and mounted; report cards, transcripts, board exports E2E.
4. **UX/a11y:** Authenticated axe/dark/touch/RTL and visual-diff run in CI for all shipped modules; no UUID labels; i18n gate; mobile shell valid.
5. **E2E:** `E2E_BACKEND_READY` job green on every PR (or nightly with badge); multi-board 3×2×500 certification standing.
6. **Release/ops:** Helm per app, staging deploy dry-run in CI, SLOs + alerts + runbooks, secret rotation, SBOM.
7. **Mobile:** Flutter semantics pass; device evidence or dated waiver.

Scoreboards may claim **program production-ready** only when every non-waived gap above is `DONE`.

---

## 7. Sources

- Repo verification (2026-09-08): `apps/api-gateway/src/{app,domain-plugins,platform-admin-ui-plugin}.ts`, `.github/workflows/{ci,deploy}.yml`, `db/sql/`, `packages/backend/*`, `apps/web/e2e/*`, `infrastructure/helm/`.
- Fable 5.1 explores: SIS/Academics, Campus ops, Platform, UX/a11y.
- `docs/plans/PEER_GAP_CLOSURE_QUEUE.md` (#1–#10 closed), `SIS_WORLD_CLASS_10_GAP_CLOSURE.md`, `ENTERPRISE_SKILLS_MAP.md`, prior `docs/audits/*`.

_Update the Status column as gaps close. This file supersedes earlier ad-hoc gap lists._
