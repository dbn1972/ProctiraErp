{{- if .Values.attendanceService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proctira.fullname" . }}-attendance-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: attendance-service
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.attendanceService.service.type }}
  selector:
    app.kubernetes.io/name: attendance-service
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.attendanceService.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
