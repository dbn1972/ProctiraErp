{{- if .Values.workflowService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "proctira.fullname" . }}-workflow-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: workflow-service
    app.kubernetes.io/component: backend
spec:
  replicas: {{ .Values.workflowService.replicaCount }}
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: workflow-service
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    {{- toYaml .Values.workflowService.strategy | nindent 4 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: workflow-service
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/part-of: proctira-platform
        app.kubernetes.io/component: backend
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "proctira.serviceAccountName" . }}
      terminationGracePeriodSeconds: 30
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: workflow-service
          image: {{ include "proctira.image" (dict "global" .Values.global "image" .Values.workflowService.image "appVersion" .Chart.AppVersion) }}
          imagePullPolicy: {{ .Values.workflowService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.workflowService.service.targetPort }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.workflowService.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "proctira.configmapName" . }}
            - secretRef:
                name: {{ include "proctira.secretName" . }}
          resources:
            {{- toYaml .Values.workflowService.resources | nindent 12 }}
          {{- with .Values.workflowService.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.workflowService.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.workflowService.startupProbe }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
