{{- if .Values.assessmentService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proctira.fullname" . }}-assessment-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: assessment-service
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.assessmentService.service.type }}
  selector:
    app.kubernetes.io/name: assessment-service
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.assessmentService.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
