{{- if .Values.reportService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proctira.fullname" . }}-report-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: report-service
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.reportService.service.type }}
  selector:
    app.kubernetes.io/name: report-service
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.reportService.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
