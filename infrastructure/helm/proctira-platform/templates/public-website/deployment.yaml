{{- if .Values.publicWebsite.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "proctira.fullname" . }}-public-website
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: public-website
    app.kubernetes.io/component: frontend
spec:
  replicas: {{ .Values.publicWebsite.replicaCount }}
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: public-website
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    {{- toYaml .Values.publicWebsite.strategy | nindent 4 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: public-website
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/part-of: proctira-platform
        app.kubernetes.io/component: frontend
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "proctira.serviceAccountName" . }}
      terminationGracePeriodSeconds: 30
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: public-website
          image: {{ include "proctira.image" (dict "global" .Values.global "image" .Values.publicWebsite.image "appVersion" .Chart.AppVersion) }}
          imagePullPolicy: {{ .Values.publicWebsite.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.publicWebsite.service.targetPort }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.publicWebsite.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "proctira.configmapName" . }}
          resources:
            {{- toYaml .Values.publicWebsite.resources | nindent 12 }}
          {{- with .Values.publicWebsite.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.publicWebsite.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
