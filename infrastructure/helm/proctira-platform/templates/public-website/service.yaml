{{- if .Values.publicWebsite.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proctira.fullname" . }}-public-website
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: public-website
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.publicWebsite.service.type }}
  selector:
    app.kubernetes.io/name: public-website
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.publicWebsite.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
