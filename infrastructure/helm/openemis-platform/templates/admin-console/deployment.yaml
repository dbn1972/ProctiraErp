{{- if .Values.adminConsole.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "proctira.fullname" . }}-admin-console
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: admin-console
    app.kubernetes.io/component: frontend
spec:
  replicas: {{ .Values.adminConsole.replicaCount }}
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: admin-console
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    {{- toYaml .Values.adminConsole.strategy | nindent 4 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: admin-console
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/part-of: proctira-platform
        app.kubernetes.io/component: frontend
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "proctira.serviceAccountName" . }}
      terminationGracePeriodSeconds: 30
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: admin-console
          image: {{ include "proctira.image" (dict "global" .Values.global "image" .Values.adminConsole.image "appVersion" .Chart.AppVersion) }}
          imagePullPolicy: {{ .Values.adminConsole.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.adminConsole.service.targetPort }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.adminConsole.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "proctira.configmapName" . }}
            - secretRef:
                name: {{ include "proctira.secretName" . }}
          resources:
            {{- toYaml .Values.adminConsole.resources | nindent 12 }}
          {{- with .Values.adminConsole.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.adminConsole.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
