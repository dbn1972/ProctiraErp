{{- if .Values.staffService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proctira.fullname" . }}-staff-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: staff-service
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.staffService.service.type }}
  selector:
    app.kubernetes.io/name: staff-service
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.staffService.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
