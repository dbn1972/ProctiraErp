{{- if .Values.registrationPortal.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proctira.fullname" . }}-registration-portal
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: registration-portal
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.registrationPortal.service.type }}
  selector:
    app.kubernetes.io/name: registration-portal
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.registrationPortal.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
