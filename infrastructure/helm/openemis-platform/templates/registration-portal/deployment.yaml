{{- if .Values.registrationPortal.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "proctira.fullname" . }}-registration-portal
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: registration-portal
    app.kubernetes.io/component: frontend
spec:
  replicas: {{ .Values.registrationPortal.replicaCount }}
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: registration-portal
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    {{- toYaml .Values.registrationPortal.strategy | nindent 4 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: registration-portal
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/part-of: proctira-platform
        app.kubernetes.io/component: frontend
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "proctira.serviceAccountName" . }}
      terminationGracePeriodSeconds: 30
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: registration-portal
          image: {{ include "proctira.image" (dict "global" .Values.global "image" .Values.registrationPortal.image "appVersion" .Chart.AppVersion) }}
          imagePullPolicy: {{ .Values.registrationPortal.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.registrationPortal.service.targetPort }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.registrationPortal.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "proctira.configmapName" . }}
            - secretRef:
                name: {{ include "proctira.secretName" . }}
          resources:
            {{- toYaml .Values.registrationPortal.resources | nindent 12 }}
          {{- with .Values.registrationPortal.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.registrationPortal.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
