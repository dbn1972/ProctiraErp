{{- if .Values.institutionService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proctira.fullname" . }}-institution-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: institution-service
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.institutionService.service.type }}
  selector:
    app.kubernetes.io/name: institution-service
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.institutionService.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
