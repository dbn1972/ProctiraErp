{{- if .Values.developerPortal.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proctira.fullname" . }}-developer-portal
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proctira.labels" . | nindent 4 }}
    app.kubernetes.io/name: developer-portal
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.developerPortal.service.type }}
  selector:
    app.kubernetes.io/name: developer-portal
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.developerPortal.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
