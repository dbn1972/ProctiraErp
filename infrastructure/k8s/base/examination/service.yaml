apiVersion: v1
kind: Service
metadata:
  name: examination-service
  namespace: proctira
  labels:
    app.kubernetes.io/name: examination-service
    app.kubernetes.io/part-of: proctira-platform
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: examination-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
