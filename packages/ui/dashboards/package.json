{
  "name": "@proctira/ui-dashboards",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable dashboard widget library (KpiCard, KpiCardWithTrend, DashboardSection, DataTableCard, MapDrillDown, RadarComparison, TimelineSchedule, TaskChecklist, ActionItemList, WelcomeBanner) for the ProctiraERP Unified Platform. Implements Design §G with skeleton loading per Property F-8 and async load announcements via <LiveRegion>.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@proctira/ui-components": "workspace:*",
    "lucide-react": "^0.487.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "recharts": "^2.15.4"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.0",
    "@testing-library/react": "^15.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.7.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
