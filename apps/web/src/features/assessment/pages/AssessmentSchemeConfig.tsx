/**
 * AssessmentSchemeConfig — configure marking schemes and weights.
 *
 * Migrated from `School Platform Design/src/app/components/AssessmentSchemeConfig.tsx`
 * per task 60.2. Real data wiring is task 60.3.
 */
export default function AssessmentSchemeConfig() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Assessment Schemes</h1>
      <p className="text-muted-foreground mt-2">
        Configure components, weights, and grading scales. Placeholder pending task 60.3.
      </p>
    </div>
  );
}
