/**
 * ForgotPassword — request a password-reset email.
 *
 * Migrated from `School Platform Design/src/app/components/auth/ForgotPassword.tsx`
 * per task 60.2. Real data wiring is task 60.3.
 */
export default function ForgotPassword() {
  return (
    <div className="mx-auto max-w-sm p-6">
      <h1 className="text-2xl font-semibold">Forgot Password</h1>
      <p className="text-muted-foreground mt-2">
        Request a password-reset email. Placeholder pending task 60.3.
      </p>
    </div>
  );
}
