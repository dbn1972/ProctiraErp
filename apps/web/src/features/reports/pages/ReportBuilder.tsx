/**
 * ReportBuilder — drag-and-drop report builder for ad-hoc reports.
 *
 * Migrated from `School Platform Design/src/app/components/ReportBuilder.tsx`
 * per task 60.2. Real data wiring is task 60.3.
 */
export default function ReportBuilder() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Report Builder</h1>
      <p className="text-muted-foreground mt-2">
        Define dimensions, measures, and filters to generate reports. Placeholder pending task 60.3.
      </p>
    </div>
  );
}
